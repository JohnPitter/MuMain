#pragma once

namespace UI::Voice
{
    // Renders a compact speaker glyph above every visible character with recent
    // voice frames being played by this client.
    void RenderSpeakingIndicators();
}
